{
  "name": "@eq/format-ui",
  "version": "0.0.1",
  "private": true,
  "description": "EQ Format - drop a messy CSV, see canonical validation. Sprint 6 first cut: Vite dev server + API middleware that talks to @eq/ai and @eq/validation. Demo-grade only; no production deploy story yet.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@eq/schemas": "workspace:*",
    "@eq/validation": "workspace:*",
    "@eq/ai": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "20.14.0",
    "typescript": "5.6.3",
    "vite": "5.4.10",
    "vitest": "2.1.5"
  }
}
